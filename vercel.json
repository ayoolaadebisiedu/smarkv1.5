{
    "buildCommand": "npm run build",
    "outputDirectory": "out",
    "rewrites": [
        {
            "source": "/api/:path*",
            "destination": "/api/index"
        }
    ]
}